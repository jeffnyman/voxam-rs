{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The one window: core events for the pipe's deliveries, the dialog for choosing a story.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
